name: "Terraform Plan Provision EC2"

on:
  workflow_dispatch:
  push:
    branches: ['master' , 'main']
  pull_request:
    branches: ['master', 'main']

permissions:
    contents: write

env:
 # verbosity setting for Terraform logs
  TF_LOG: INFO

jobs:
  plan:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./terraform/tf-ec2-jenkins

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Terraform Init
        env:
          TF_API_TOKEN: ${{ secrets.TF_API_TOKEN }}
        run: |
          terraform init
      
      - name: Terraform Format
        run: |
          terraform fmt
      
      - name: Terraform Plan
        run: |
          terraform plan

      - run: echo "🍏 The job's status is ${{ job.status }}."